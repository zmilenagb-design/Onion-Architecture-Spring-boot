# Conexión a PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/empresas_db
spring.datasource.username=postgres
spring.datasource.password=michell0205
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Puerto del servidor
server.port=8080

# Logs con nombre de hilo visible
logging.pattern.console=%d{HH:mm:ss} [%t] %-5level %logger{36} - %msg%n
logging.level.com.example.empresasapi=INFO

# JWT Configuration
app.jwt.secret=clave_secreta_muy_segura_minimo_32_caracteres_aqui
app.jwt.expiration=3600000